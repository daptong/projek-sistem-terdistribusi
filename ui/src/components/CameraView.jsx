import React from 'react'

export default function CameraView({camera}){
  return (
    <div>
      <h4>{camera.id}</h4>
      <div>Camera view placeholder (use RTSP/snapshot integration)</div>
    </div>
  )
}
